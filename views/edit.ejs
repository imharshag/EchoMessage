<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-gray-300 flex flex-col min-h-screen">

    <!-- Header -->
    <header class="w-full bg-gray-800 p-4 shadow-lg flex justify-between items-center">
        <div class="flex items-center">
            <!-- Logo -->
            <div class="whisper-logo text-white text-2xl font-bold">
                <!-- Replace with your logo -->
                <span class="text-indigo-400">Whisper &#x1F92B;</span>
            </div>
        </div>
        <div class="flex items-center">
            <!-- Back Button with Bag Logo -->
            <a class="bg-gray-700 hover:bg-gray-600 text-blue-400 rounded-md px-4 py-2 text-sm transition-transform transform hover:scale-105" href="/profile">Back</a>
        </div>
    </header>

    <!-- Main Content -->
    <main class="flex-1 p-6 max-w-7xl mx-auto">
        <h5 class="text-2xl font-bold text-indigo-400 mb-5">Edit Your Post</h5>
        
        <form method="post" action="/update/<%= post._id %>" class="bg-gray-800 p-8 rounded-lg shadow-lg">
            <textarea placeholder="What's on your mind?" class="w-full h-60 p-4 outline-none resize-none bg-gray-700 border-2 border-gray-600 rounded-md text-gray-300 focus:ring-2 focus:ring-indigo-500" name="content"><%= post.content %></textarea>
            <input class="mt-4 px-6 py-3 bg-yellow-500 text-black rounded-md cursor-pointer hover:bg-yellow-600 transition-transform transform hover:scale-105" type="submit" value="Update Your Post">
        </form>
    </main>

</body>
</html>
